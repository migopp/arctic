[package]
name = "art"
version = "0.1.0"
edition = "2021"

[features]
default = []
loom = ["dep:loom"]

[dependencies]
ribbit.git = "https://github.com/nwtnni/ribbit.git"
loom = { version = "0.7", optional = true }

[dev-dependencies]
lincheck.path = "lincheck"
proptest = "1.6"
